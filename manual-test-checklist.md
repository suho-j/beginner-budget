# V1 수동 테스트 체크리스트

상세 기준은 [테스트 계획](docs/TEST_PLAN.md)을 따릅니다.

## 미리보기 격리 준비

- [ ] `docs/supabase-preview-setup.sql`이 운영 테이블·함수·정책을 update·delete·alter하지 않는지 확인했다.
- [ ] 운영 `budget_settings`, `transactions`를 백업하고 사용자별 행 수를 읽기 전용 기준으로 기록했다.
- [ ] 비표준 ID의 결정적 매핑과 후보끼리의 ID 충돌 감사를 실행했고 충돌이 0건이다.
- [ ] 최초 적용 전 기존 preview 행 충돌이 0건인지 확인했다. 재적용이면 기존 preview 편집을 덮어쓰지 않는지 별도 검토했다.
- [ ] preview SQL을 적용하고 두 테이블·RLS·권한·트리거·5인자 CAS·sample RPC 제거를 확인했다.
- [ ] 운영→preview 복사 행 수와 ID 매핑을 대조했고 운영 두 테이블의 행·값은 바뀌지 않았다.
- [ ] 미리보기 SQL 적용·QA에는 운영 단일 배타적 쓰기 창을 열지 않았다.
- [ ] 미리보기에서 원래 선택 월의 총예산과 네 카테고리 예산을 기록했다.
- [ ] 미리보기 JSON 내보내기 백업을 받았다.
- [ ] 불변 접두사 `<marker> = QA-V1-<timestamp>`와 생성 직후 거래 ID를 기록할 준비가 됐다.
- [ ] 미리보기 복사본 스모크에서는 JSON 가져오기·전체 초기화·샘플 데이터를 사용하지 않는다.

## 자동 검증

- [ ] storage, transactions, cloud, ui, app, 테스트 실행기의 `node --check`가 모두 통과한다.
- [ ] `node tests/run-tests.cjs`가 `65 tests passed`로 끝난다.
- [ ] `git diff --check`와 `git diff --check origin/master..HEAD`가 통과한다.

## 로그인과 준비 상태

- [ ] 로그인 전 금융 데이터가 보이지 않고 모든 쓰기 버튼이 잠겨 있다.
- [ ] 로그인 후 클라우드 다운로드가 끝나야 쓰기 버튼이 활성화된다.
- [ ] 다운로드 실패 시 기존 상태를 저장할 수 없고 다시 불러오기·로그아웃 안내가 보인다.
- [ ] 로컬과 미리보기 `/beginner-budget-preview/v1/`에서 `개발 화면 · 운영 데이터 복사본`과 운영 미반영 안내가 보인다.
- [ ] 운영 `/beginner-budget/`에서는 복사본 안내가 숨겨진다.

## 탭과 월 탐색

- [ ] 홈·내역·캘린더·설정 탭이 한 번에 하나만 열린다.
- [ ] 이전 달·이번 달·다음 달 이동 결과를 네 탭이 함께 사용한다.
- [ ] 예산 시작일이 1일이 아니어도 표시 기간과 거래 포함 범위가 일치한다.
- [ ] 시작일 29~31일에서 2월 경계와 다음 예산 월이 올바르다.

## QA 거래 추가·필터·수정·삭제

- [ ] 홈에서 `<marker>-추가` 메모와 쉼표 금액으로 거래 한 건을 추가하고 생성된 ID를 즉시 기록한다.
- [ ] Supabase 성공 후에만 합계와 목록이 바뀐다.
- [ ] 내역에서 월·유형·검색어·카테고리 필터를 조합해 QA 거래만 찾는다.
- [ ] 같은 거래의 날짜, 유형, 카테고리, 금액을 수정하고 메모는 `<marker>-수정`처럼 접두사를 유지하며 ID가 그대로인지 확인한다.
- [ ] 날짜를 다른 예산 월로 옮기면 현재 목록에서 사라진 이유를 안내한다.
- [ ] 캘린더에서 수정 날짜의 수입·지출 합계, 건수, 상세 거래를 확인한다.
- [ ] 캘린더 상세에서도 수정 창과 삭제를 사용할 수 있다.
- [ ] 같은 QA 거래를 삭제한 뒤 최종 클라우드 다운로드와 JSON 내보내기로 모든 월에서 QA ID와 접두사가 0건인지 확인한다.
- [ ] `preview_transactions`에서 `where id = '<QA ID>' or memo like '<marker>%'` 조건의 건수가 0인지 확인한다.

## 예산과 충돌

- [ ] 총예산과 카테고리 예산을 저장하고 성공 뒤 화면이 갱신된다.
- [ ] 저장한 예산을 시작 전에 기록한 값으로 정확히 복원한다.
- [ ] 격리 환경에서 다른 브라우저가 먼저 바꾼 거래·예산·전체 상태는 충돌로 거부되고 로컬 상태가 유지된다.
- [ ] 충돌 뒤 클라우드를 다시 불러오면 최신 값이 표시된다.

## 키보드·접근성·모바일

- [ ] Tab으로 네 탭과 모든 핵심 입력·버튼에 도달한다.
- [ ] 탭에서 방향키·Home·End로 포커스를 옮기고 Enter 또는 Space로 활성화한다.
- [ ] 수정 창의 첫 입력에 포커스가 가며 Escape·취소 후 호출 지점으로 돌아간다.
- [ ] 저장·오류·필터 결과·캘린더 상세 건수가 live region으로 전달된다.
- [ ] 360×800에서 문서 전체 가로 스크롤이 없고 캘린더 날짜 버튼을 선택할 수 있다.
- [ ] 포커스 표시가 고정 경고·탭에 가려지지 않는다.
- [ ] 브라우저 콘솔 오류가 0건이다.

## 종료와 원복

- [ ] 클라우드를 다시 다운로드해 QA ID와 불변 접두사가 모든 월에서 0건인지 확인했다.
- [ ] 미리보기 총예산과 카테고리 예산이 시작 전 스냅샷과 같다.
- [ ] preview 두 테이블의 사용자별 최종 행 수를 기록했고 운영 테이블에는 QA 변경이 없다.
- [ ] 로그아웃 후 금융 데이터와 필터가 화면에서 사라지고 쓰기가 잠긴다.
- [ ] 검증 로그에 URL, 정확한 안전 소스 SHA, 불변 QA 접두사·QA ID, 예산 원복 값, 콘솔 오류 수를 남겼다.

## 사용자가 URL을 승인한 뒤 운영 승격

- [ ] 운영 SQL 적용 직전에 단일 배타적 쓰기 창을 열고 모든 구버전 운영 탭과 쓰기를 중단했다.
- [ ] 창 안에서 운영 백업·사용자별 행 수·결정적 ID 매핑·두 충돌 감사를 authoritative 자료로 다시 확정했다.
- [ ] `docs/supabase-setup.sql`과 선택 URL 매니페스트의 정확한 안전 소스 SHA만 운영에 적용했다.
- [ ] 운영 URL을 새로 열어 복사본 안내가 숨겨지고 클라우드 다운로드·인증 스모크·운영 QA 정리·예산 원복이 끝난 뒤 창을 닫았다.
